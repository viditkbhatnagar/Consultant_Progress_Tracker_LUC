# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage

# Production
build/
dist/

# Environment Variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
server/.env
client/.env

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
*.log

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# CSV data (optional - uncomment if you don't want to commit CSV files)
# server/data/*.csv

# Database backups
db_backup_*/
# Backfill / autoclose JSON snapshots produced by maintenance scripts.
server/scripts/backups/

# Build artifacts
*.tgz
*.zip
.env
.env.local

# Docs RAG build artifact — regenerated on every ingest, not source
server/program-docs-manifest.json

# Prototype files
consultant-tracker-v2.html

# Raw spreadsheet exports — keep out of the public repo (likely PII).
# Block-list added during Export Center anonymization audit. Use
# `git add -f path.xlsx` to commit a deliberately public xlsx
# (e.g. a documentation template).
*.xlsx
